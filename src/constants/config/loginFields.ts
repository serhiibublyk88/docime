//  constants/config/loginFields.ts
import { fieldConfigs } from "./fieldConfigs";
import { FieldConfig } from "../../types/constantsTypes"; 

export const loginFields: FieldConfig[] = [
  fieldConfigs.email,
  fieldConfigs.password,
];
