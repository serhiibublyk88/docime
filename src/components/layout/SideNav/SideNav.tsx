import styles from "./SideNav.module.css";

const SideNav = () => {
  return (
    <div className={styles.sideNav}>
      {/* Пока пустой контейнер для навигации */}
    </div>
  );
};

export default SideNav;
